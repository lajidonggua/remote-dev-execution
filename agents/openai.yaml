interface:
  display_name: "Remote Development Execution"
  short_description: "Connect to and run commands in the authoritative environment"
  default_prompt: "Use $remote-dev-execution to connect to or run the appropriate command in my authoritative development environment."
